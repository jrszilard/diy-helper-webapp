# Fixerator Rebrand — Status & Remaining Work

Tracks the migration from the internal "DIY Helper" / aspirational "Crafted" branding to **Fixerator**, with the **Fix the FIX-3000** mascot. Design source: Claude Design handoff bundle (May 2026).

---

## Done

| Area | Surface |
|---|---|
| Brand tokens | `--bot-eye`, `--bot-glow`, `--bot-chrome` in `app/globals.css` |
| Mascot animations | `.fix-float`, `.fix-bot.shooting` muzzle/nail/recoil, `.fix-thinking-dot`, reduced-motion gated |
| Mascot SVG | `components/FixBot.tsx` — 4 expressions (default / winking / computing / terminator), optional nailgun rig + floating |
| Logo lockup | `components/AppLogo.tsx` — replaces `crafted-logo.png`, supports `theme` + `size` |
| Landing hero | `app/page.tsx` — "Hi, I'm Fix. I'm here to terminate your project." + floating nailgun bot |
| Status copy | `Completed → Terminated` (label only; DB enum unchanged) |
| Page metadata | Title / description / OG site name → Fixerator |
| Empty state mascot | `components/ui/EmptyState.tsx` `fixBot` prop, wired in `ProjectsSidebar` |
| "Fix says" callout | `components/ui/FixSays.tsx` primitive |
| Email templates | Header brand + footer + subject lines → Fixerator |
| Notification sender | Resend `from` display name → Fixerator (domain unchanged) |
| Stripe products | Pro tier + expert subscription product names → Fixerator |
| Expert embed badge | Logo initials `DH → FX`, alt text, "Powered by Fixerator" footer |
| Material/Shopping exports | PDF + text footers → "Generated by Fixerator" |
| Share page | CTA + footer → Fixerator |
| Comparison + About | "Why Fixerator?", "Experts on Fixerator" |
| Design system page | New mascot gallery, FixSays section, robot-accent swatches, Fix empty-state variant, AppLogo theme/size demo |

---

## Remaining

### 1. Image assets (need raster/vector regeneration — not codable)

| File | Status |
|---|---|
| `public/favicon.ico` | Still old DIY Helper |
| `public/apple-touch-icon.png` | Still old DIY Helper |
| `public/og-image.png` | Used by social card meta; still old branding |
| `public/crafted-logo.png` | Orphaned — no longer referenced. Safe to delete once we confirm no caches |

Action: regenerate from the `FixBot` SVG in `components/FixBot.tsx` + the FIXERATOR wordmark. Target sizes: 32×32 + 48×48 (favicon), 180×180 (apple-touch), 1200×630 (og-image).

### 2. Infra-bound strings

| String | Where | Status |
|---|---|---|
| `fixerator.com` host | `.env.example`, `app/layout.tsx` `metadataBase`, `lib/notifications.ts` `EMAIL_DOMAIN` fallback | ✅ Swapped (Cloudflare custom domain provisioned May 2026). Resend domain verification must be re-run for the new hostname. |
| `diy-helper-*` localStorage keys | `hooks/useChat.ts`, `lib/guestStorage.ts`, e2e tests | Deliberately preserved — renaming wipes guest carts, in-flight conversations, and project drafts for every existing user. Phase B refactor. |
| `diy-helper[a-z0-9-]*\.vercel\.app` regex | `lib/config.ts` | Preserved — Vercel project slug is the URL; renaming invalidates all preview URLs and CLI history. |

### 3. Optional polish (design doc doesn't dictate placement)

- Wire `FixSays` into a live surface — candidates: post-AI-response callout in chat, "permit alert" in materials list, contextual hint above project plans.
- Swap the in-chat loading `<Spinner>` for `fix-thinking-dot` + "Fix is computing…" mono label.
- Add small `FIX-3000` mono chrome labels to project cards (design doc shows this as a card detail).
- Add a rust-background variant to the AppLogo design-system demo (currently dark + light only).
